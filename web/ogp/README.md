# Web版 OGP 画像

MVP では `default.png` 1 枚を固定で使用する（日付・スコアは焼き込まない。`og:description` の文字列にのみ含める）。

## ファイル

- `default.png` — 1200×630 px 推奨。Brain Boost のロゴ・キャッチコピー・背景色のみ。Week 4 で実物を配置。
- 動的生成版（日付・スコアを画像に埋める）は v1.1 以降で Cloudflare Workers 等を使って実装する。

## 現状

まだ画像ファイルを配置していません。Week 4 のシェアURL実装タスクで配置します。
